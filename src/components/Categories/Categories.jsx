import React from 'react';

import { Title } from '../Title/Title';

export const Categories = () => {
  return <section className="categories">
    <Title titleName={`Shop by category`} subClass={``}/>
  </section>
};
